# 15.08.2026

- Changed the update system mechanism from using an `update.txt` file to a link within the `.exe` build's `.iss` file
- Prepared documentation in the `docs` folder reflecting the project version at the time of the fork (`1.3.1.1`)
- Localized all files into the custom language — `RU`
- Upgraded the project to .NET 4.8
- Moved all tests into `.Tests` sub-projects and organized them into functional blocks
- Restored the previously removed `FLUtils` to ensure the build launches and the project operates correctly
